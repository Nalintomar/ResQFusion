PORT=4000
JWT_SECRET=change-this-in-production
ML_URL=http://localhost:5001
ML_TIMEOUT_MS=4000

# Leave blank to use the in-memory stores (fine for demo/dev). Set these for real deployments.
MONGO_URI=
MONGO_DB=resqfusion
DATABASE_URL=

# Leave blank to use the built-in simulators.
OPENWEATHER_API_KEY=
X_BEARER_TOKEN=

S3_BUCKET=
AWS_REGION=ap-south-1

INGEST_INTERVAL_MS=5000
AUTO_START_PIPELINE=true
SIM_SEED=2026
CORS_ORIGIN=*
